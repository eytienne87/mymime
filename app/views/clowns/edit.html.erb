<h1>This is the edit a mime page</h1>

<%= simple_form_for(@clown) do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :years_of_experience %>
    <%= f.input :gender %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
